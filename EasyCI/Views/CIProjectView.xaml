<Window x:Class="EasyCI.Views.CIProjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyCI.Views"
        xmlns:viewmodels="clr-namespace:EasyCI.ViewModels"
        mc:Ignorable="d"
        Title="Criar Projeto CI" Height="550" Width="800"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Criar Projeto de Integração Contínua" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Associe um repositório Git com um container Docker para criar um pipeline de integração contínua." TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Formulário -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Nome do Projeto -->
            <Label Grid.Row="0" Grid.Column="0" Content="Nome do Projeto:" VerticalAlignment="Center" Margin="0,0,10,15"/>
            <TextBox x:Name="TxtName" Grid.Row="0" Grid.Column="1" Margin="0,0,0,15" Padding="5" Text="{Binding Name}"/>

            <!-- Repositório Git -->
            <Label Grid.Row="1" Grid.Column="0" Content="Repositório Git:" VerticalAlignment="Center" Margin="0,0,10,15"/>
            <ComboBox x:Name="CmbGitRepository" Grid.Row="1" Grid.Column="1" Margin="0,0,0,15" Padding="5"
                      ItemsSource="{Binding GitRepositories}"
                      SelectedItem="{Binding SelectedGitRepository}"
                      DisplayMemberPath="Name"/>

            <!-- Container Docker -->
            <Label Grid.Row="2" Grid.Column="0" Content="Container Docker:" VerticalAlignment="Center" Margin="0,0,10,15"/>
            <ComboBox x:Name="CmbDockerContainer" Grid.Row="2" Grid.Column="1" Margin="0,0,0,15" Padding="5"
                      ItemsSource="{Binding DockerContainers}"
                      SelectedItem="{Binding SelectedDockerContainer}"
                      DisplayMemberPath="Name"/>

            <!-- Arquivo Docker Compose -->
            <Label Grid.Row="3" Grid.Column="0" Content="Arquivo Compose:" VerticalAlignment="Center" Margin="0,0,10,15"/>
            <TextBox x:Name="TxtComposeFilePath" Grid.Row="3" Grid.Column="1" Margin="0,0,0,15" Padding="5" Text="{Binding ComposeFilePath}"/>

            <!-- Build Automático -->
            <Label Grid.Row="4" Grid.Column="0" Content="Build Automático:" VerticalAlignment="Center" Margin="0,0,10,15"/>
            <CheckBox x:Name="ChkAutoBuild" Grid.Row="4" Grid.Column="1" Content="Executar build automaticamente quando houver mudanças no repositório" IsChecked="{Binding AutoBuild}" VerticalAlignment="Center" Margin="0,0,0,15"/>

            <!-- Status -->
            <Label Grid.Row="5" Grid.Column="0" Content="Status:" VerticalAlignment="Center" Margin="0,0,10,15"/>
            <CheckBox x:Name="ChkIsActive" Grid.Row="5" Grid.Column="1" Content="Ativo" IsChecked="{Binding IsActive}" VerticalAlignment="Center" Margin="0,0,0,15"/>
        </Grid>

        <!-- Botões -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="BtnCancel" Content="Cancelar" Padding="15,8" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button x:Name="BtnSave" Content="Criar Projeto" Padding="15,8" Background="#0078D7" Foreground="White" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
